{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "db",
            "group": "db",
            "database": "${workspaceFolder:task}/server/db.sqlite3"
        }
    ]
}